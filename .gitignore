# secrets — never commit
.env
*.key

# python
.venv/
__pycache__/
*.pyc
.pytest_cache/

# data / logs
*.log
data/cache/
agent/loop.pid

# reference repos studied locally (third-party, not part of this project)
repos/

# research papers kept locally as reference (see commit messages for analysis)
*.pdf

